# Config file is updated from mirte_bringup/config/telemetrix/... on push to GitHub
Only update this config file if using this package directly

If you're using `mirte_bringup` launch files, edit them over there.

[mirte_user_config.yaml](../../mirte_bringup/config/telemetrix/mirte_user_config.yaml)

[mirte_master_config.yaml](../../mirte_bringup/config/telemetrix/mirte_master_config.yaml)