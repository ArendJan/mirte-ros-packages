# Config file is updated from mirte_bringup/config/control/... on push to GitHub
Only update this config file if using this package directly

If you're using `mirte_bringup` launch files, edit them over there.

[control_master](../../../../mirte_bringup/config/control/control_master.yaml) 

[mirte_base_control](../../../../mirte_bringup/config/control/mirte_base_control.yaml)
